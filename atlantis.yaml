version: 3
automerge: true
parallel_apply: true
parallel_plan: true
projects:
  - autoplan:
      enabled: true
      when_modified:
        - '*.hcl'
        - '*.tf*'
        - 'files'
    dir: aws/dev/vpc
  - autoplan:
      enabled: true
      when_modified:
        - '*.hcl'
        - '*.tf*'
        - 'files'
    dir: aws/dev/eks
